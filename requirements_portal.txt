# Minimal requirements for portal.py (Flask clinical portal)
# Much lighter than requirements.txt — no torch/transformers needed for the portal

# Web framework
flask>=3.0.0
werkzeug>=3.0.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# PDF → image conversion (Tier 0)
PyMuPDF>=1.23.0

# Image preprocessing (Tier 0 — optional, graceful degradation if absent)
opencv-python-headless>=4.9.0

# Utilities
python-dateutil>=2.8.0
numpy>=1.24.0
